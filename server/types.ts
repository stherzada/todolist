export interface Project {
    id: string
    nome: string
    descricao: string
    preco: number
    tipo: string
    categoria: string
  }
  export interface User {
    id: string
    nome: string
    email: string
    senha: string
  }